import { makeStyles } from "@material-ui/core/styles"

export const useStyles = makeStyles(() => ({
  root: {
    display: "flex",
    flex: "1 0 70%",
    flexWrap: "wrap",
    flexDirection: "row",
    alignItems: "stretch"
  }
}))