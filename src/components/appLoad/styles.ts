import { makeStyles } from "@material-ui/core/styles"

export const useStyles = makeStyles({
  root: {
    backgroundColor: "#fff",
    height: "100vh"
  },
  image: {
    width: "100%",
    maxHeight: "90vh"
  }
})